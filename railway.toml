[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npm run start:army14:railway"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ALWAYS"
numReplicas = 1
sleepApplication = false
