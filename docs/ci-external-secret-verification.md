# CI external secret verification

This marker exists to trigger the supervision workflow on the revision that reads the `EASY_GITHUB_TOKEN` repository secret for the real external GitHub write proof.

The workflow itself is the authoritative evidence; this file contains no credential material.
