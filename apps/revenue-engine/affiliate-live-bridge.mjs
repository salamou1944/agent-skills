import { appendFile, mkdir, readFile } from 'node:fs/promises';
import { mkdirSync, readFileSync, writeFileSync, rmSync, statSync } from 'node:fs';
import { dirname } from 'node:path';
import { createServer } from 'node:http';
import { createEngine } from './revenue-engine.mjs';
import { createElevenLabsAffiliateAdapter } from './elevenlabs-affiliate-adapter.mjs';

const port = Number(process.env.MONY_REVENUE_PORT || 8796);
const ledgerPath = process.env.MONY_REVENUE_LEDGER_PATH || '.easy/mony/revenue-ledger.jsonl';
const ledgerLockPath = `${ledgerPath}.lock`;
const ledgerLockStaleMs = 120_000;
let heldLedgerLockToken = null;
const postbackSecret = String(process.env.MONY_PARTNERSTACK_POSTBACK_SECRET || '').trim();
const apiKey = String(process.env.EASY_OPENAI_API_KEY || '').trim();
const textModel = String(process.env.MONY_TEXT_MODEL || 'gpt-4.1-mini').trim();
const engine = createEngine({ mode: 'live', providers: { 'elevenlabs-affiliate': createElevenLabsAffiliateAdapter() } });
const requestWindow = new Map();

async function ensureLedger() { await mkdir(dirname(ledgerPath), { recursive: true }); }
async function appendLedger(record) { await ensureLedger(); await appendFile(ledgerPath, `${JSON.stringify(record)}\n`, 'utf8'); }
function processStartToken(pid=process.pid) { try { const stat=readFileSync(`/proc/${pid}/stat`,'utf8'); return stat.slice(stat.lastIndexOf(')')+2).trim().split(/\\s+/)[19]||null; } catch { return null; } }
function lockOwnerAlive(owner) { if (!owner || Number(owner.pid)<=0 || !owner.startToken) return false; return processStartToken(Number(owner.pid))===String(owner.startToken); }
function acquireLedgerLock() { mkdirSync(dirname(ledgerLockPath),{recursive:true}); for(let i=0;i<100;i+=1){ try { mkdirSync(ledgerLockPath,{mode:0o700}); writeFileSync(`${ledgerLockPath}/owner`,JSON.stringify({pid:process.pid,startToken:processStartToken(),createdAt:Date.now()}),{mode:0o600}); return; } catch(error) { if(error.code!=='EEXIST') throw error; let recoverable=false; try { const owner=JSON.parse(readFileSync(`${ledgerLockPath}/owner`,'utf8')); const age=Date.now()-Number(owner.createdAt); recoverable=age>ledgerLockStaleMs&&!lockOwnerAlive(owner); } catch { try { const age=Date.now()-statSync(ledgerLockPath).mtimeMs; recoverable=age>ledgerLockStaleMs; } catch {} } if(recoverable) { try { rmSync(ledgerLockPath,{recursive:true,force:true}); } catch {} } } } throw new Error('mony_ledger_lock_timeout'); }
function releaseLedgerLock() { if(!heldLedgerLockToken)return; try { const owner=JSON.parse(readFileSync(`${ledgerLockPath}/owner`,'utf8')); if(owner?.token===heldLedgerLockToken)rmSync(ledgerLockPath,{recursive:true,force:true}); } catch {} heldLedgerLockToken=null; }
async function readLedger() { try { const raw = await readFile(ledgerPath, 'utf8'); return raw.split('\n').filter(Boolean).map((line) => JSON.parse(line)); } catch (error) { if (error.code === 'ENOENT') return []; throw error; } }
async function body(req) { let raw=''; for await (const chunk of req) { raw+=chunk; if(raw.length>1_000_000) throw new Error('request_too_large'); } return raw?JSON.parse(raw):{}; }
function json(res,status,value,extraHeaders={}) { res.writeHead(status,{'content-type':'application/json; charset=utf-8','cache-control':'no-store','x-content-type-options':'nosniff',...extraHeaders}); res.end(JSON.stringify(value)); }
function html(res,status,value) { res.writeHead(status,{'content-type':'text/html; charset=utf-8','cache-control':'no-store','x-content-type-options':'nosniff'}); res.end(value); }
function authorizedPostback(pathname) { return Boolean(postbackSecret)&&pathname===`/api/revenue/partnerstack/${encodeURIComponent(postbackSecret)}`; }
function rewardIsCashEligible(data={}) { const rewardStatus=String(data.reward_status||'').toLowerCase(); const paymentStatus=String(data.payment_status||'').toLowerCase(); return rewardStatus==='paid'||paymentStatus==='available'||paymentStatus==='withdrawn'; }
async function handleReward(event) { if(!event||!['reward.created','reward.updated'].includes(event.event)) return {status:'ignored',reason:'unsupported_event'}; const data=event.data||{}; if(!rewardIsCashEligible(data)) return {status:'ignored',reason:'reward_not_cash_eligible',rewardStatus:data.reward_status||null,paymentStatus:data.payment_status||null}; const amountCents=Number(data.amount); if(!Number.isFinite(amountCents)||amountCents<=0)return{status:'rejected',reason:'invalid_reward_amount'}; const externalEventId=String(data.key||'').trim(); if(!externalEventId)return{status:'rejected',reason:'missing_reward_key'}; acquireLedgerLock(); try { const existing=(await readLedger()).find((record)=>record.externalEventId===externalEventId); if(existing){const sameAmount=Number(existing.amount)*100===amountCents;const sameCurrency=String(existing.currency||'USD').toUpperCase()==='USD';const sameProvider=existing.provider==='elevenlabs-affiliate';if(!sameAmount||!sameCurrency||!sameProvider)return{status:'rejected',reason:'reward_event_conflict',record:existing};return{status:'already_recorded',record:existing};} const recorded=engine.recordRevenue({confirmed:true,provider:'elevenlabs-affiliate',path:'affiliate',amount:amountCents/100,currency:'USD',externalEventId}); if(recorded.status!=='recorded')return recorded; const record={...recorded,rewardStatus:data.reward_status||null,paymentStatus:data.payment_status||null,source:data.source||null,partnershipKey:data.partnership_key||null,companyKey:data.company?.key||null}; await appendLedger(record); return record; } finally { releaseLedgerLock(); } }
function rateLimitKey(req){const forwarded=String(req.headers['x-forwarded-for']||'').split(',')[0].trim();return forwarded||String(req.socket.remoteAddress||'local');}
function checkRateLimit(req){const key=rateLimitKey(req),now=Date.now(),windowMs=60_000;const current=requestWindow.get(key)||{started:now,count:0};if(now-current.started>=windowMs){current.started=now;current.count=0;}current.count+=1;requestWindow.set(key,current);if(current.count>12)return Math.ceil((windowMs-(now-current.started))/1000);return 0;}
async function openAIJson(system,user,schema){
  if(!apiKey)throw Object.assign(new Error('mony_ai_provider_not_configured'),{code:'provider_credentials_missing',status:503});
  const response=await fetch('https://api.openai.com/v1/responses',{method:'POST',headers:{authorization:`Bearer ${apiKey}`,'content-type':'application/json'},body:JSON.stringify({model:textModel,store:false,input:[{role:'system',content:[{type:'input_text',text:system}]},{role:'user',content:[{type:'input_text',text:user}]}],text:{format:{type:'json_schema',name:'mony_work_result',strict:true,schema}}})});
  const retryAfter=response.headers.get('retry-after'),raw=await response.text();let parsed={};try{parsed=raw?JSON.parse(raw):{};}catch{parsed={raw};}
  if(!response.ok){const error=Object.assign(new Error(parsed?.error?.message||`openai_http_${response.status}`),{status:response.status,code:`provider_http_${response.status}`});if(retryAfter)error.retryAfter=retryAfter;throw error;}
  const text=parsed?.output_text||parsed?.output?.flatMap((item)=>item?.content||[]).find((part)=>part?.type==='output_text')?.text;if(!text)throw Object.assign(new Error('provider_invalid_output'),{status:502,code:'provider_invalid_output'});try{return JSON.parse(text);}catch{throw Object.assign(new Error('provider_invalid_json'),{status:502,code:'provider_invalid_output'});}
}
function fallbackWork(task,source){
  const clean=source.replace(/\s+/g,' ').trim();
  if(task==='product')return{title:`${clean.split(/[.!?]/)[0].slice(0,70)} — Product`,short_description:`A clear product listing built only from the supplied facts: ${clean.slice(0,180)}.`,description:`${clean}\n\nMONY has not added unsupported product claims.`,selling_points:clean.split(/[,.;]/).map((x)=>x.trim()).filter(Boolean).slice(0,6),ad_copy:`Discover ${clean.slice(0,120)}. Explore the verified product details and choose what fits your needs.`,cta:'Learn more',audience:'Customers matching the supplied product context',cautions:['Verify every commercial claim before publishing.','Do not add unsupported pricing, certifications or guarantees.']};
  if(task==='voice')return{title:'MONY Voiceover Script',cleaned_script:clean,delivery_notes:'Natural pace, short pauses between ideas, emphasize key nouns and the final call to action.',hook:clean.split(/[.!?]/)[0].slice(0,160),cta:'Learn more'};
  return{offer_name:'MONY Productized Service',promise:'A concrete deliverable built around the supplied business problem.',deliverables:['Discovery summary','Defined deliverable','Revision boundary','Delivery handoff'],turnaround:'Define before accepting the client.',price_positioning:'Quote after confirming scope, inputs and revision limits.',outreach_message:`I can turn this problem into a defined deliverable with a clear scope and handoff. Based on what you shared: ${clean.slice(0,240)}`,qualification_questions:['What exact outcome is required?','What inputs/assets are available?','Who approves the final result?','What deadline and revision limit apply?']};
}
const schemas={product:{type:'object',additionalProperties:false,properties:{title:{type:'string'},short_description:{type:'string'},description:{type:'string'},selling_points:{type:'array',items:{type:'string'}},ad_copy:{type:'string'},cta:{type:'string'},audience:{type:'string'},cautions:{type:'array',items:{type:'string'}}},required:['title','short_description','description','selling_points','ad_copy','cta','audience','cautions']},voice:{type:'object',additionalProperties:false,properties:{title:{type:'string'},cleaned_script:{type:'string'},delivery_notes:{type:'string'},hook:{type:'string'},cta:{type:'string'}},required:['title','cleaned_script','delivery_notes','hook','cta']},offer:{type:'object',additionalProperties:false,properties:{offer_name:{type:'string'},promise:{type:'string'},deliverables:{type:'array',items:{type:'string'}},turnaround:{type:'string'},price_positioning:{type:'string'},outreach_message:{type:'string'},qualification_questions:{type:'array',items:{type:'string'}}},required:['offer_name','promise','deliverables','turnaround','price_positioning','outreach_message','qualification_questions']}};
async function runWork(input={}){const task=String(input.task||'').trim(),source=String(input.source||'').trim();if(!['product','voice','offer'].includes(task))throw Object.assign(new Error('unsupported_mony_task'),{status:422});if(source.length<12||source.length>20_000)throw Object.assign(new Error('source_must_be_12_to_20000_characters'),{status:422});const prompts={product:['You are MONY Product Content. Produce commercially useful copy from only supplied product facts. Never invent claims.',`Create a complete product-content package from these facts:\n${source}`],voice:['You are MONY Voice Studio. Prepare natural voiceover copy without inventing claims.',`Prepare this material for professional voiceover:\n${source}`],offer:['You are MONY Client Offer Builder. Turn the supplied business problem into a concrete sellable service offer without inventing client facts or guarantees.',`Build a service offer from this problem/context:\n${source}`]};try{return{task,provider:`openai:${textModel}`,result:await openAIJson(prompts[task][0],prompts[task][1],schemas[task]),fallback:false};}catch(error){if(error.status===429||error.code==='provider_http_429'){throw Object.assign(new Error('mony_ai_provider_rate_limited'),{status:503,code:'provider_http_429',retryAfter:error.retryAfter,blocked:true});}throw error;}}
const workbench=()=>`<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>MONY — AI Workbench</title><style>*{box-sizing:border-box}body{margin:0;font-family:system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif;background:#050b14;color:#edf7ff}main{max-width:1080px;margin:auto;padding:24px 18px 60px}.nav{display:flex;justify-content:space-between;align-items:center;padding:8px 2px 26px}.logo{font-size:30px;font-weight:950}.live{font-size:12px;padding:7px 10px;border:1px solid #31506d;border-radius:999px;color:#bfe9ff}.hero{padding:32px 24px;border:1px solid #29465f;border-radius:26px;background:#0a1725}.hero h1{font-size:clamp(34px,7vw,58px);line-height:1;margin:8px 0 12px;letter-spacing:-2px}.hero p{color:#9eb4c9;line-height:1.65;max-width:760px}.grid{display:grid;grid-template-columns:repeat(3,1fr);gap:14px;margin-top:16px}.card{padding:20px;border:1px solid #243f56;border-radius:20px;background:#08131f}.card h2{margin:0 0 8px;font-size:19px}.card p{color:#91a7bb;line-height:1.5;font-size:14px}.card button,.partner{width:100%;padding:13px 15px;border:0;border-radius:11px;background:#73dcff;color:#031019;font-weight:900;cursor:pointer;margin-top:14px}.workspace{margin-top:16px;padding:22px;border:1px solid #29465f;border-radius:20px;background:#07111c}.workspace h2{margin-top:0}.workspace textarea{width:100%;min-height:170px;resize:vertical;background:#050c14;color:#eef8ff;border:1px solid #29465f;border-radius:12px;padding:14px;font:inherit;line-height:1.5}.actions{display:flex;gap:10px;flex-wrap:wrap}.actions button{width:auto;min-width:180px}.status{color:#8fa8be;font-size:13px;margin-top:12px}.result{margin-top:16px;display:none;background:#050c14;border:1px solid #29465f;border-radius:14px;padding:16px;white-space:pre-wrap;line-height:1.6;overflow:auto}.result.show{display:block}.partner{display:block;text-align:center;text-decoration:none;margin-top:16px}.disclosure{font-size:12px;color:#6f8498;line-height:1.5;margin-top:20px}@media(max-width:760px){.grid{grid-template-columns:1fr}.hero{padding:26px 18px}}</style></head><body><main><div class="nav"><div class="logo">MONY</div><div class="live">AI Workbench</div></div><section class="hero"><div>REAL CUSTOMER WORKSPACE</div><h1>Do the work here. Use providers only when they add value.</h1><p>MONY is the interface: create product content, prepare voice scripts, or turn a business problem into a sellable service offer. AI runs behind MONY when the provider is available, with a fail-closed provider boundary when rate-limited.</p></section><section class="grid"><div class="card"><h2>Product Content</h2><p>Turn product facts into a ready listing, selling points, ad copy and CTA.</p><button data-task="product">Use Product Content</button></div><div class="card"><h2>Voice Studio</h2><p>Turn a script or rough idea into clean, voiceover-ready copy.</p><button data-task="voice">Use Voice Studio</button></div><div class="card"><h2>Client Offer</h2><p>Turn a business problem into a concrete service package and outreach message.</p><button data-task="offer">Build Client Offer</button></div></section><section class="workspace"><h2 id="taskTitle">Choose a service</h2><p id="hint">Select a service, then paste the material you want MONY to work on.</p><textarea id="source" placeholder="Your product facts, script, or client problem..."></textarea><div class="actions"><button id="run" disabled>Run MONY</button><a id="partner" class="partner" href="#" target="_blank" rel="sponsored noopener noreferrer" style="display:none">Open verified AI voice partner</a></div><div id="status" class="status"></div><pre id="result" class="result"></pre></section><p class="disclosure">MONY uses AI providers behind the product interface. Affiliate links are disclosed and optional; revenue is recorded only after a provider confirms a qualifying reward.</p></main><script>let task='';const title=document.getElementById('taskTitle'),hint=document.getElementById('hint'),source=document.getElementById('source'),run=document.getElementById('run'),status=document.getElementById('status'),result=document.getElementById('result'),partner=document.getElementById('partner');const config={product:['Product Content','Paste product facts, features, materials, audience and verified claims.'],voice:['Voice Studio','Paste the script, notes or rough copy for narration.'],offer:['Client Offer','Describe the business problem, target client and service context.']};document.querySelectorAll('[data-task]').forEach(b=>b.onclick=()=>{task=b.dataset.task;title.textContent=config[task][0];hint.textContent=config[task][1];run.disabled=false;source.focus();result.classList.remove('show');partner.style.display=task==='voice'?'block':'none';if(task==='voice')loadPartner()});async function loadPartner(){try{const r=await fetch('/api/revenue/affiliate-link',{cache:'no-store'}),b=await r.json();if(r.ok&&b.partnerTrackingUrl){partner.href=b.partnerTrackingUrl;status.textContent='Verified partner link ready. Use MONY first, then continue to the partner if needed.'}else throw new Error()}catch{partner.style.display='none';status.textContent='Partner link temporarily unavailable; MONY services remain available.'}}run.onclick=async()=>{if(!task||source.value.trim().length<12)return;run.disabled=true;status.textContent='MONY is processing...';result.classList.remove('show');try{const r=await fetch('/api/revenue/mony/work',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({task,source:source.value})});const b=await r.json();if(!r.ok)throw Object.assign(new Error(b.error||'request_failed'),{retryAfter:r.headers.get('retry-after')});result.textContent=JSON.stringify(b.result,null,2);result.classList.add('show');status.textContent=b.fallback?'Provider unavailable; no successful AI result was recorded.':'Completed by MONY AI.'}catch(e){status.textContent=e.retryAfter?'Provider rate limit. Retry in '+e.retryAfter+'s.':'MONY could not complete this run: '+e.message}finally{run.disabled=false}};</script></body></html>`;
const server=createServer(async(req,res)=>{try{const url=new URL(req.url,`http://${req.headers.host||'localhost'}`);if(req.method==='GET'&&url.pathname==='/api/revenue/health'){const adapter=createElevenLabsAffiliateAdapter(),affiliate=await adapter.healthCheck();return json(res,200,{ok:true,mode:'live',affiliate,ai:{configured:Boolean(apiKey),model:textModel,fallback:false},postbackConfigured:Boolean(postbackSecret),ledgerPath,rule:'Only PartnerStack reward events that are cash-eligible are recorded as revenue.'});}if(req.method==='GET'&&url.pathname==='/api/revenue/affiliate-link'){const adapter=createElevenLabsAffiliateAdapter(),health=await adapter.healthCheck();if(!health.ok)return json(res,503,{error:'affiliate_unavailable'});const partnerTrackingUrl=(await adapter.execute({action:'tracking_url'})).trackingUrl;return json(res,200,{provider:'mony',trackingUrl:partnerTrackingUrl,partnerTrackingUrl,workbenchUrl:'/api/revenue/mony/workbench',disclosureRequired:true});}if(req.method==='GET'&&url.pathname==='/api/revenue/mony/workbench')return html(res,200,workbench());if(req.method==='POST'&&url.pathname==='/api/revenue/mony/work'){const retryAfter=checkRateLimit(req);if(retryAfter)return json(res,429,{error:'rate_limited',retryAfter},{'retry-after':String(retryAfter)});return json(res,200,await runWork(await body(req)));}if(req.method==='GET'&&url.pathname==='/api/revenue/ledger'){const records=await readLedger(),total=records.reduce((sum,item)=>sum+Number(item.amount||0),0);return json(res,200,{count:records.length,total,currency:'USD',records});}if(req.method==='POST'&&authorizedPostback(url.pathname)){const result=await handleReward(await body(req));return json(res,result.status==='recorded'?201:result.status==='rejected'?422:200,result);}return json(res,404,{error:'not_found'});}catch(error){const status=Number(error.status||400),headers=error.retryAfter?{'retry-after':String(error.retryAfter)}:{};return json(res,status,{error:error.message||'request_failed',code:error.code||'request_failed'},headers);}});
await ensureLedger();
server.listen(port,'127.0.0.1',()=>console.log(JSON.stringify({service:'mony-affiliate-live-bridge',port,live:true,partnerstackPostback:Boolean(postbackSecret),aiConfigured:Boolean(apiKey),textModel,fallback:false,providerFailureMode:'fail-closed'})));
